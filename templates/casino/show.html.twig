{% extends 'base.html.twig' %}

{% block title %}Casino{% endblock %}
{% block body %}
    <h1>Casino</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ casino.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ casino.name }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ casino.brand }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ casino.adress }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ casino.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('casino_index') }}">back to list</a>

    <a href="{{ path('casino_edit', {'id': casino.id}) }}">edit</a>

    {{ include('casino/_delete_form.html.twig') }}
{{form}}
{% endblock %}
