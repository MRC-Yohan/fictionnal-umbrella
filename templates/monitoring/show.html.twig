{% extends 'base.html.twig' %}

{% block title %}Monitoring{% endblock %}

{% block body %}
    <h1>Monitoring</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monitoring.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ monitoring.type }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ monitoring.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('monitoring_index') }}">back to list</a>

    <a href="{{ path('monitoring_edit', {'id': monitoring.id}) }}">edit</a>

    {{ include('monitoring/_delete_form.html.twig') }}
{% endblock %}
